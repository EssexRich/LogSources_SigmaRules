title: Detect ARP Poisoning
id: 50e641ac-50e6-40e6-a0e6-50e641ac0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1200
references:
  - https://github.com/splunk/security_content/blob/develop/detections/network/detect_arp_poisoning.yml
author: splunk (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1200
logsource:
  product: network
  service: firewall
detection:
  selection:
    # Original splunk detection logic
    # `cisco_networks` facility="PM" mnemonic="ERR_DISABLE" disable_cause="arp-inspection"
    #   | eval src_interface=src_int_prefix_long+src_int_suffix | stats min(_time) AS firstTime
    #   max(_time) AS lastTime count BY host src_interface | `security_content_ctime(firstTime)`|`security_content_ctime(lastTime)`|
    #   `detect_arp_poisoning_filter`
  condition: selection
level: medium
