title: Cisco SA - Access to Anonymizer Services
id: 7e9bc5cb-7e9b-4e9b-ae9b-7e9bc5cb0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1090.003
references:
  - https://github.com/splunk/security_content/blob/develop/detections/network/cisco_sa___access_to_anonymizer_services.yml
author: splunk (via IncidentBuddy)
date: 2026-06-21
tags:
  - attack.t1090.003
logsource:
  product: network
  service: firewall
detection:
  selection:
    # Original splunk detection logic
    # -
    #     `cisco_secure_access_dns`
    #     action = "allowed" category= "*anonymizer*"
    #     | fillnull
    #     | stats count min(_time) as firstTime max(_time) as lastTime values(domain) as domain values(query) as query values(reply_code) as reply_code values(record_type) as record_type by src_ip src_external_ip user identity_type action category sourcetype
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `cisco_sa___access_to_anonymizer_services_filter`
  condition: selection
level: medium
