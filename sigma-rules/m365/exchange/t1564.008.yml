title: M365 Exchange Inbox Rule with Obfuscated Name
id: 585c69c0-585c-485c-a85c-585c69c00000
status: stable
description: Detection rule for MITRE ATT&CK technique T1564.008
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/integrations/o365/defense_evasion_exchange_inbox_rule_obfuscated_name.toml
author: elastic (via IncidentBuddy)
date: 2026-08-16
tags:
  - attack.t1564.008
logsource:
  product: m365
  service: exchange
detection:
  selection:
    # Original elastic detection logic
    # from logs-o365.audit-* metadata _id, _version, _index
    # | where
    #     data_stream.dataset == "o365.audit" and
    #     event.provider == "Exchange" and
    #     event.action in ("New-InboxRule", "Set-InboxRule") and
    #     event.outcome == "success" and
    #     o365.audit.ObjectId is not null
    # | grok o365.audit.ObjectId """.*\\\\(?<Esql.inbox_rule_name>.*)$"""
    # // only special chars in inbox rule name
    # | where Esql.inbox_rule_name rlike """[!@#$%^&*()_+={[\]|\\:;"'<,>.?/~` \-]+"""
    # | keep
    #     @timestamp,
    #     _id,
    #     _version,
    #     _index,
    #     Esql.inbox_rule_name,
    #     o365.audit.ObjectId,
    #     o365.audit.UserId,
    #     o365.audit.ApplicationId,
    #     user.name,
    #     user.domain,
    #     event.action,
    #     source.ip,
    #     source.as.number,
    #     source.as.organization.name,
    #     o365.audit.Parameters.ForwardTo,
    #     o365.audit.Parameters.ForwardAsAttachmentTo,
    #     o365.audit.Parameters.RedirectTo
  condition: selection
level: medium
