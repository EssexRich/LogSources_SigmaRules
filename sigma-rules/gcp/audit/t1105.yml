title: GKE Pod Exec with Curl or Wget to HTTPS
id: 27208ceb-2720-4720-a720-27208ceb0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1105
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/integrations/gcp/execution_gcp_gke_pod_exec_curl_wget_https.toml
author: elastic (via IncidentBuddy)
date: 2026-08-16
tags:
  - attack.t1105
logsource:
  product: gcp
  service: audit
detection:
  selection:
    # Original elastic detection logic
    # data_stream.dataset:gcp.audit and service.name:"k8s.io" and event.outcome:success and
    # event.type:start and
    # event.action:("io.k8s.core.v1.pods.exec.create" or "io.k8s.core.v1.pods.exec.get") and
    # gcp.audit.labels.command.gke.io/command:(
    #   (*curl*https* or *wget*https*) and not (
    #     */.well-known/jwks.json* or */.well-known/openid-configuration* or
    #     */api/v1/health* or */healthz* or */livez* or */readyz* or
    #     */openid-connect/certs* or */openid/v1/jwks* or
    #     *127.0.0.1* or *kubernetes.default.svc* or *localhost*
    #   )
    # )
  condition: selection
level: medium
