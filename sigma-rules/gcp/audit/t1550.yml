title: GKE API Server Proxying Request to Kubelet
id: 4825d68c-4825-4825-a825-4825d68c0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1550
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/integrations/gcp/privilege_escalation_gcp_gke_api_proxy_to_node.toml
author: elastic (via IncidentBuddy)
date: 2026-08-30
tags:
  - attack.t1550
logsource:
  product: gcp
  service: audit
detection:
  selection:
    # Original elastic detection logic
    # data_stream.dataset:gcp.audit and service.name:"k8s.io" and event.outcome:success and
    # event.action:(
    #   "io.k8s.core.v1.nodes.proxy.get" or
    #   "io.k8s.core.v1.nodes.proxy.create"
    # ) and
    # not gcp.audit.resource_name:(*metrics* or *healthz* or *stats/summary* or *elastic-agent* or *configz*) and
    # not client.user.email:(
    #   "system:kube-controller-manager" or
    #   "system:kube-scheduler" or
    #   system\:serviceaccount\:kube-system\:* or
    #   system\:node\:*
    # )
  condition: selection
level: medium
