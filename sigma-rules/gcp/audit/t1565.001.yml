title: GKE CoreDNS or Kube-DNS Configuration Modified
id: 28eb09e7-28eb-48eb-a8eb-28eb09e70000
status: stable
description: Detection rule for MITRE ATT&CK technique T1565.001
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/integrations/gcp/impact_gcp_gke_coredns_or_kube_dns_configuration_modified.toml
author: elastic (via IncidentBuddy)
date: 2026-08-23
tags:
  - attack.t1565.001
logsource:
  product: gcp
  service: audit
detection:
  selection:
    # Original elastic detection logic
    # data_stream.dataset:gcp.audit and service.name:"k8s.io" and event.outcome:success and
    # event.action:("io.k8s.core.v1.configmaps.update" or "io.k8s.core.v1.configmaps.patch" or "io.k8s.core.v1.configmaps.delete") and
    # orchestrator.resource.name:("coredns" or "kube-dns" or "coredns-custom") and orchestrator.namespace:"kube-system" and
    # gcp.audit.labels.authorization.k8s.io/decision:"allow" and
    # not user.email:(
    #   "system:serviceaccount:kube-system:coredns" or
    #   "system:serviceaccount:kube-system:kube-dns" or
    #   system\:node* or system\:serviceaccount\:kube-system*
    # )
  condition: selection
level: medium
