title: Attempt to Install Root Certificate
id: 750b71c1-750b-450b-a50b-750b71c10000
status: stable
description: Detection rule for MITRE ATT&CK technique T1553.004
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/defense_evasion_install_root_certificate.toml
author: elastic (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1553.004
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "macos" and event.type in ("start", "process_started") and
    #   process.name == "security" and process.args like "add-trusted-cert" and
    #   (process.parent.name like~ ("osascript", "bash", "sh", "zsh", "Terminal", "Python*") or (process.parent.code_signature.exists == false or process.parent.code_signature.trusted == false))
  condition: selection
level: medium
