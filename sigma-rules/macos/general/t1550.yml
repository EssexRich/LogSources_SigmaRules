title: Potential Kerberos Attack via Bifrost
id: 6b71e2ee-6b71-4b71-ab71-6b71e2ee0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1550
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/lateral_movement_credential_access_kerberos_bifrostconsole.toml
author: elastic (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1550
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "macos" and event.type in ("start", "process_started") and
    #  process.args like~ "-action" and (process.args like~ ("-kerberoast", "askhash", "asktgs", "asktgt", "s4u") or process.args like~ ("-ticket", "ptt") or process.args like~ "dump") and process.args like~ ("tickets", "keytab")
  condition: selection
level: medium
