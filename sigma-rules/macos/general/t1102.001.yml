title: Potential EtherHiding C2 via Curl JSON-RPC Request
id: 5302b961-5302-4302-a302-5302b9610000
status: stable
description: Detection rule for MITRE ATT&CK technique T1102.001
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/command_and_control_etherhiding_c2_via_blockchain_rpc_request.toml
author: elastic (via IncidentBuddy)
date: 2026-09-13
tags:
  - attack.t1102.001
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "macos" and event.type == "start" and event.action == "exec" and
    #   process.name in ("curl", "nscurl") and
    #   process.parent.name in ("osascript", "bash", "sh", "zsh") and
    #   process.command_line like~ ("*eth_call*", "*\"jsonrpc\"*")
  condition: selection
level: medium
