title: Launch Service Creation and Immediate Loading
id: 760137f1-7601-4601-a601-760137f10000
status: stable
description: Detection rule for MITRE ATT&CK technique T1543
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/persistence_creation_change_launch_agents_file.toml
author: elastic (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1543
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # sequence by host.id with maxspan=30s
    #  [file where host.os.type == "macos" and event.action == "launch_daemon"] by process.entity_id
    #  [process where host.os.type == "macos" and event.type in ("start", "process_started") and process.name == "launchctl" and process.args == "load"] by process.parent.entity_id
  condition: selection
level: medium
