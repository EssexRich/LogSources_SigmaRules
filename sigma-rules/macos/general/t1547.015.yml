title: Creation of Hidden Login Item via Apple Script
id: 41a086e9-41a0-41a0-a1a0-41a086e90000
status: stable
description: Detection rule for MITRE ATT&CK technique T1547.015
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/persistence_creation_hidden_login_item_osascript.toml
author: elastic (via IncidentBuddy)
date: 2026-05-17
tags:
  - attack.t1547.015
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "macos" and event.type in ("start", "process_started") and process.name == "osascript" and
    #  process.command_line : "osascript*login item*hidden:true*"
  condition: selection
level: medium
