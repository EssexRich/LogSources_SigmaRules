title: SSFileCopySender Executed as Root
id: 5353cb4f-5353-4353-a353-5353cb4f0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1068
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/initial_access_ssfilecopysender_root_execution.toml
author: elastic (via IncidentBuddy)
date: 2026-09-06
tags:
  - attack.t1068
logsource:
  product: macos
  service: general
detection:
  selection:
    # Original elastic detection logic
    # from logs-endpoint.events.process-* METADATA _id, _index, _version
    # | WHERE host.os.type == "macos"
    #     AND event.type == "start"
    #     AND process.name == "SSFileCopySender"
    #     AND KQL(""" process.args : "0" AND process.args : "80" """)
    # | KEEP _id, _version, _index,
    #     @timestamp,
    #     data_stream.namespace,
    #     host.name,
    #     host.id,
    #     user.id,
    #     user.name,
    #     process.name,
    #     process.entity_id,
    #     process.parent.name,
    #     process.command_line
    # | SORT @timestamp DESC
    # | LIMIT 100
  condition: selection
level: medium
