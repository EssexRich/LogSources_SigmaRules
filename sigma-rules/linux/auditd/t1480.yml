title: Linux Auditd AI CLI Permission Override Activated
id: 2de5c835-2de5-4de5-ade5-2de5c8350000
status: stable
description: Detection rule for MITRE ATT&CK technique T1480
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/linux_auditd_ai_cli_permission_override_activated.yml
author: splunk (via IncidentBuddy)
date: 2026-03-15
tags:
  - attack.t1480
logsource:
  product: linux
  service: auditd
detection:
  selection:
    # Original splunk detection logic
    # -
    #     `linux_auditd` (proctitle = "*gemini*" AND proctitle IN ("*--yolo*", "*-y *")) OR
    #     (proctitle = "*claude*" AND proctitle= "*--dangerously-skip-permissions*")
    #     | rename host as dest
    #     | stats count min(_time) as firstTime max(_time) as lastTime
    #       BY proctitle dest
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)` | `linux_auditd_ai_cli_permission_override_activated_filter`
  condition: selection
level: medium
