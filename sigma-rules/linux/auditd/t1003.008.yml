title: Linux Auditd Possible Access To Credential Files
id: 55b9e282-55b9-45b9-a5b9-55b9e2820000
status: stable
description: Detection rule for MITRE ATT&CK technique T1003.008
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/linux_auditd_possible_access_to_credential_files.yml
author: splunk (via IncidentBuddy)
date: 2026-07-12
tags:
  - attack.t1003.008
logsource:
  product: linux
  service: auditd
detection:
  selection:
    # Original splunk detection logic
    # `linux_auditd`  proctitle IN (\"*shadow*\", \"*passwd*\") AND proctitle IN (\"*cat *\", \"*nano *\", \"*vim *\", \"*vi *\")\n  | rename host as dest\n  | stats count min(_time) as firstTime max(_time) as lastTime\n    BY proctitle dest\n  | `security_content_ctime(firstTime)`\n  | `security_content_ctime(lastTime)`\n  | `linux_auditd_possible_access_to_credential_files_filter`
  condition: selection
level: medium
