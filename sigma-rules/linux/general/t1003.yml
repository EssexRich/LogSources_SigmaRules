title: Potential Linux Credential Dumping via Unshadow
id: 42e1b173-42e1-42e1-a2e1-42e1b1730000
status: stable
description: Detection rule for MITRE ATT&CK technique T1003
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/credential_access_credential_dumping.toml
author: elastic (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1003
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "linux" and event.type == "start" and event.action in ("exec", "exec_event", "start", "ProcessRollup2") and
    # process.name == "unshadow" and process.args_count >= 3
  condition: selection
level: medium
