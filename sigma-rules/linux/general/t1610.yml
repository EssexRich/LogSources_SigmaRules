title: Kubectl Apply Pod from URL
id: 233598c5-2335-4335-a335-233598c50000
status: stable
description: Detection rule for MITRE ATT&CK technique T1610
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/execution_kubectl_apply_pod_from_url.toml
author: elastic (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1610
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "linux" and event.type == "start" and
    # event.action in ("exec", "exec_event", "start", "ProcessRollup2", "process_started", "executed") and
    # process.name == "kubectl" and process.args == "apply" and
    # process.args like ("http://*", "https://*") and
    # not process.command_line like ("*download.elastic.co*", "*github.com/kubernetes-sigs/*")
  condition: selection
level: medium
