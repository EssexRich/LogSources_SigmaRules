title: Deprecated - Creation of Kernel Module
id: 4fde764d-4fde-4fde-afde-4fde764d0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1547
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/_deprecated/persistence_creation_of_kernel_module.toml
author: elastic (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1547
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # file where host.os.type == "linux" and event.type in ("change", "creation") and file.path : "/lib/modules/*" and
    # file.extension == "ko" and not process.name : (
    #   "dpkg", "systemd", "falcon-sensor*", "dnf", "yum", "rpm", "cp"
    # )
  condition: selection
level: medium
