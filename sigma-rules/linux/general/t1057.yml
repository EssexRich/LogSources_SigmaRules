title: Potential Linux Credential Dumping via Proc Filesystem
id: 5fb8cd8a-5fb8-4fb8-afb8-5fb8cd8a0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1057
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/credential_access_proc_credential_dumping.toml
author: elastic (via IncidentBuddy)
date: 2026-08-02
tags:
  - attack.t1057
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # sequence by host.id, process.parent.name with maxspan=1m
    #   [process where host.os.type == "linux" and process.name == "ps" and event.action in ("exec", "start", "exec_event")
    #    and process.args in ("-eo", "pid", "command")]
    #   [process where host.os.type == "linux" and process.name == "strings" and event.action in ("exec", "start", "exec_event")
    #    and process.args : "/tmp/*"]
  condition: selection
level: medium
