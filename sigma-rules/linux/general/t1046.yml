title: Suspicious Use of /dev/tcp
id: 5c2f84b5-5c2f-4c2f-ac2f-5c2f84b50000
status: stable
description: Detection rule for MITRE ATT&CK technique T1046
references:
  - https://github.com/SigmaHQ/sigma/blob/master/rules/linux/builtin/lnx_susp_dev_tcp.yml
author: sigma (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1046
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original sigma detection logic
    #     keywords:
    #         - 'cat </dev/tcp/'
    #         - 'exec 3<>/dev/tcp/'
    #         - 'echo >/dev/tcp/'
    #         - 'bash -i >& /dev/tcp/'
    #         - 'sh -i >& /dev/udp/'
    #         - '0<&196;exec 196<>/dev/tcp/'
    #         - 'exec 5<>/dev/tcp/'
    #         - '(sh)0>/dev/tcp/'
    #         - 'bash -c ''bash -i >& /dev/tcp/'
    #         - 'echo -e ''#!/bin/bash\nbash -i >& /dev/tcp/'
    #     condition: keywords
    # 
  condition: selection
level: medium
