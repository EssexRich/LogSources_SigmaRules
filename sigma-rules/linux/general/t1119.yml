title: Potential Database Dumping Activity
id: 2091e613-2091-4091-a091-2091e6130000
status: stable
description: Detection rule for MITRE ATT&CK technique T1119
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/exfiltration_potential_database_dumping.toml
author: elastic (via IncidentBuddy)
date: 2026-06-28
tags:
  - attack.t1119
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "linux" and event.type == "start" and
    # event.action in ("exec", "exec_event", "start", "ProcessRollup2") and
    # process.name in ("pg_dump", "pg_dumpall", "mysqldump", "mariadb-dump", "mongodump")
  condition: selection
level: medium
