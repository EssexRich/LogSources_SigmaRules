title: Linux init (PID 1) Secret Dump via GDB
id: 44fa7d32-44fa-44fa-a4fa-44fa7d320000
status: stable
description: Detection rule for MITRE ATT&CK technique T1003.007
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/credential_access_gdb_init_process_hooking.toml
author: elastic (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1003.007
logsource:
  product: linux
  service: general
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "linux" and event.type == "start" and event.action in ("exec", "exec_event", "start", "ProcessRollup2", "executed", "process_started") and
    # process.name == "gdb" and process.args in ("--pid", "-p") and process.args == "1"
  condition: selection
level: medium
