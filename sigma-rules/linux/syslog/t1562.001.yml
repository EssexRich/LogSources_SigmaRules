title: Attempt to Disable Syslog Service
id: 43b64db6-43b6-43b6-a3b6-43b64db60000
status: stable
description: Detection rule for MITRE ATT&CK technique T1562.001
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/linux/defense_evasion_attempt_to_disable_syslog_service.toml
author: elastic (via IncidentBuddy)
date: 2026-08-09
tags:
  - attack.t1562.001
logsource:
  product: linux
  service: syslog
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "linux" and event.type == "start" and
    # event.action in ("exec", "exec_event", "start", "ProcessRollup2") and (
    #   (process.name == "service" and process.args == "stop") or
    #   (process.name == "chkconfig" and process.args == "off") or
    #   (process.name == "update-rc.d" and process.args in ("remove", "disable")) or
    #   (process.name == "systemctl" and process.args in ("disable", "stop", "kill", "mask"))
    # ) and
    # process.args in ("syslog", "rsyslog", "syslog-ng", "syslog.service", "rsyslog.service", "syslog-ng.service") and
    # not (
    #   process.parent.name == "rsyslog-rotate" or
    #   process.args == "HUP"
    # )
  condition: selection
level: medium
