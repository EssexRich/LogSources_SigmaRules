title: Apache Threading Error
id: 41c5427d-41c5-41c5-a1c5-41c5427d0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1210
references:
  - https://github.com/SigmaHQ/sigma/blob/master/rules/web/product/apache/web_apache_threading_error.yml
author: sigma (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1210
logsource:
  product: web
  service: proxy
detection:
  selection:
    # Original sigma detection logic
    #     keywords:
    #         - '__pthread_tpp_change_priority: Assertion `new_prio == -1 || (new_prio >= fifo_min_prio && new_prio <= fifo_max_prio)'
    #     condition: keywords
    # 
  condition: selection
level: medium
