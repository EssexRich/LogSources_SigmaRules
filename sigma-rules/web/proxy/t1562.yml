title: ESXi Encryption Settings Modified
id: 5d816bd9-5d81-4d81-ad81-5d816bd90000
status: stable
description: Detection rule for MITRE ATT&CK technique T1562
references:
  - https://github.com/splunk/security_content/blob/develop/detections/application/esxi_encryption_settings_modified.yml
author: splunk (via IncidentBuddy)
date: 2026-05-10
tags:
  - attack.t1562
logsource:
  product: web
  service: proxy
detection:
  selection:
    # Original splunk detection logic
    # `esxi_syslog` Message="*system settings encryption set*" NOT Message="*shell.*" Message IN ("* -s *", "* -e *","*--require-secure-boot*", "*require-exec-installed-only*", "execInstalledOnly") | rex field=_raw "Z (?<dest>[\w\.]*)\s.*\]: \[(?<user>\w+)\]:(?<command>.+)" | stats min(_time) as firstTime max(_time) as lastTime count by dest user command | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)` | `esxi_encryption_settings_modified_filter`
  condition: selection
level: medium
