title: Cisco ASA - Packet Capture Activity
id: 3a47e816-3a47-4a47-aa47-3a47e8160000
status: stable
description: Detection rule for MITRE ATT&CK technique T1040
references:
  - https://github.com/splunk/security_content/blob/develop/detections/application/cisco_asa___packet_capture_activity.yml
author: splunk (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1040
logsource:
  product: web
  service: proxy
detection:
  selection:
    # Original splunk detection logic
    # `cisco_asa`
    #   message_id IN (111008, 111010)
    #   command IN ("capture *")
    #   | fillnull
    #   | stats count
    #           earliest(_time) as firstTime
    #           latest(_time) as lastTime
    #           values(user) as user
    #           values(action) as action
    #           values(message_id) as message_id
    #           values(command) as command
    #           values(src_ip) as src_ip
    #           values(process_name) as process_name
    #     by host
    #   | `security_content_ctime(firstTime)`
    #   | `security_content_ctime(lastTime)`
    #   | `cisco_asa___packet_capture_activity_filter`
  condition: selection
level: medium
