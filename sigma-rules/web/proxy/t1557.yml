title: WebProxy Settings Modification
id: 63acccd8-63ac-43ac-a3ac-63acccd80000
status: stable
description: Detection rule for MITRE ATT&CK technique T1557
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/credential_access_mitm_localhost_webproxy.toml
author: elastic (via IncidentBuddy)
date: 2026-07-19
tags:
  - attack.t1557
logsource:
  product: web
  service: proxy
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "macos" and event.type in ("start", "process_started") and event.action == "exec" and
    #  process.name == "networksetup" and process.args like~ ("-setwebproxy", "-setsecurewebproxy", "-setautoproxyurl") and
    #  (process.parent.name like~ ("osascript", "bash", "sh", "zsh", "Terminal", "Python*") or (process.parent.code_signature.exists == false or process.parent.code_signature.trusted == false))
  condition: selection
level: medium
