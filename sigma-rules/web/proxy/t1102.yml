title: Linux Ngrok Reverse Proxy Usage
id: 6d478de7-6d47-4d47-ad47-6d478de70000
status: stable
description: Detection rule for MITRE ATT&CK technique T1102
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/linux_ngrok_reverse_proxy_usage.yml
author: splunk (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1102
logsource:
  product: web
  service: proxy
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` count min(_time) as firstTime max(_time)
    #   as lastTime from datamodel=Endpoint.Processes where Processes.process_name=ngrok
    #   Processes.process IN ("*start*", "*--config*","*http*","*authtoken*", "*http*",
    #   "*tcp*") by Processes.action Processes.dest Processes.original_file_name Processes.parent_process
    #   Processes.parent_process_exec Processes.parent_process_guid Processes.parent_process_id
    #   Processes.parent_process_name Processes.parent_process_path Processes.process Processes.process_exec
    #   Processes.process_guid Processes.process_hash Processes.process_id Processes.process_integrity_level
    #   Processes.process_name Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    #   | `drop_dm_object_name(Processes)` | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`
    #   | `linux_ngrok_reverse_proxy_usage_filter`
  condition: selection
level: medium
