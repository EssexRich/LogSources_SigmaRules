title: Sqlite Module In Temp Folder
id: 6c4ba6a6-6c4b-4c4b-ac4b-6c4ba6a60000
status: stable
description: Detection rule for MITRE ATT&CK technique T1005
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/sqlite_module_in_temp_folder.yml
author: splunk (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1005
logsource:
  product: windows
  service: sysmon
detection:
  selection:
    # Original splunk detection logic
    # `sysmon` EventCode=11 (TargetFilename = "*\\sqlite32.dll" OR TargetFilename
    #   = "*\\sqlite64.dll") (TargetFilename = "*\\temp\\*") | stats count min(_time) as
    #   firstTime max(_time) as lastTime by action dest file_name file_path  process_guid
    #   process_id user_id vendor_product | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`
    #   | `sqlite_module_in_temp_folder_filter`
  condition: selection
level: medium
