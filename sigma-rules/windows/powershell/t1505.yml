title: Microsoft Exchange Worker Spawning Suspicious Processes
id: 17f0850a-17f0-47f0-a7f0-17f0850a0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1505
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/windows/initial_access_suspicious_ms_exchange_worker_child_process.toml
author: elastic (via IncidentBuddy)
date: 2026-05-17
tags:
  - attack.t1505
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "windows" and event.type == "start" and
    #   process.parent.name : "w3wp.exe" and process.parent.args : "MSExchange*AppPool" and
    #   (
    #     (process.name : ("cmd.exe", "powershell.exe", "pwsh.exe", "powershell_ise.exe") or
    #     ?process.pe.original_file_name in ("Cmd.Exe", "PowerShell.EXE", "pwsh.dll", "powershell_ise.EXE"))
    #   )
  condition: selection
level: medium
