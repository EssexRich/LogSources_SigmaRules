title: Windows ESX Admins Group Creation via PowerShell
id: 55d81872-55d8-45d8-a5d8-55d818720000
status: stable
description: Detection rule for MITRE ATT&CK technique T1136.002
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/windows_esx_admins_group_creation_via_powershell.yml
author: splunk (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1136.002
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original splunk detection logic
    # `powershell` EventCode=4104 (ScriptBlockText="*New-ADGroup*" OR ScriptBlockText="*New-LocalGroup*")
    #   ScriptBlockText="*ESX Admins*" | fillnull | stats count min(_time) as firstTime
    #   max(_time) as lastTime by dest signature signature_id user_id vendor_product EventID
    #   Guid Opcode Name Path ProcessID ScriptBlockId ScriptBlockText | `security_content_ctime(firstTime)`
    #   | `security_content_ctime(lastTime)` | `windows_esx_admins_group_creation_via_powershell_filter`
  condition: selection
level: medium
