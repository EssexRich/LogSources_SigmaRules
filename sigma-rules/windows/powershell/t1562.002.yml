title: Clearing Windows Event Logs
id: 515334b0-5153-4153-a153-515334b00000
status: stable
description: Detection rule for MITRE ATT&CK technique T1562.002
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/windows/defense_evasion_clearing_windows_event_logs.toml
author: elastic (via IncidentBuddy)
date: 2026-05-17
tags:
  - attack.t1562.002
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "windows" and event.type == "start" and
    # (
    #   (
    #     (process.name : "wevtutil.exe" or ?process.pe.original_file_name == "wevtutil.exe") and
    #     process.args : ("/e:false", "cl", "clear-log")
    #   ) or
    #   (
    #     (
    #       process.name : ("powershell.exe", "pwsh.exe", "powershell_ise.exe") or
    #       ?process.pe.original_file_name in ("PowerShell.EXE", "pwsh.dll", "powershell_ise.EXE")
    #     ) and
    #     process.args : "Clear-EventLog"
    #   )
    # )
  condition: selection
level: medium
