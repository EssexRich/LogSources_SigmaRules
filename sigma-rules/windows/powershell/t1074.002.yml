title: Remote File Copy to a Hidden Share
id: 2a4f250b-2a4f-4a4f-aa4f-2a4f250b0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1074.002
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/windows/lateral_movement_remote_file_copy_hidden_share.toml
author: elastic (via IncidentBuddy)
date: 2026-07-05
tags:
  - attack.t1074.002
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "windows" and event.type == "start" and user.id != "S-1-5-18" and
    #   process.name : ("cmd.exe", "powershell.exe") and
    #   process.command_line : "*\\\\*\\*$*" and process.command_line : ("* copy*", "* move*", "* cp *", "* mv *")
  condition: selection
level: medium
