title: PowerShell Script with Veeam Credential Access Capabilities
id: 04cdd295-04cd-44cd-a4cd-04cdd2950000
status: stable
description: Detection rule for MITRE ATT&CK technique T1213
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/windows/credential_access_posh_veeam_sql.toml
author: elastic (via IncidentBuddy)
date: 2026-07-26
tags:
  - attack.t1213
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original elastic detection logic
    # event.category:process and host.os.type:windows and
    #   powershell.file.script_block_text : (
    #     (
    #       "[dbo].[Credentials]" and
    #       ("Veeam" or "VeeamBackup")
    #     ) or
    #     "ProtectedStorage]::GetLocalString"
    #   )
  condition: selection
level: medium
