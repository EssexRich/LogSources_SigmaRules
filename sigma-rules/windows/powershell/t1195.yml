title: Command Execution via SolarWinds Process
id: 3e6969fd-3e69-4e69-ae69-3e6969fd0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1195
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/windows/execution_apt_solarwinds_backdoor_child_cmd_powershell.toml
author: elastic (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1195
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original elastic detection logic
    # process where host.os.type == "windows" and event.type == "start" and process.name: ("cmd.exe", "powershell.exe") and
    # process.parent.name: (
    #      "ConfigurationWizard*.exe",
    #      "NetflowDatabaseMaintenance*.exe",
    #      "NetFlowService*.exe",
    #      "SolarWinds.Administration*.exe",
    #      "SolarWinds.Collector.Service*.exe",
    #      "SolarwindsDiagnostics*.exe"
    #      )
  condition: selection
level: medium
