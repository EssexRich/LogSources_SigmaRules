title: Windows SCCM Smsexec Spawned a Suspicious Child Process
id: 2301eb4a-2301-4301-a301-2301eb4a0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1574.002
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/windows_sccm_smsexec_spawned_a_suspicious_child_process.yml
author: splunk (via IncidentBuddy)
date: 2026-09-20
tags:
  - attack.t1574.002
logsource:
  product: windows
  service: powershell
detection:
  selection:
    # Original splunk detection logic
    # -
    #     | tstats `security_content_summariesonly`
    #       count min(_time) as firstTime
    #             max(_time) as lastTime
    # 
    #     from datamodel=Endpoint.Processes where
    # 
    #     Processes.parent_process_name="smsexec.exe"
    #     Processes.process_name IN (
    #         "bitsadmin.exe",
    #         "certutil.exe",
    #         "cmd.exe",
    #         "conhost.exe",
    #         "cscript.exe",
    #         "curl.exe",
    #         "mshta.exe",
    #         "net.exe",
    #         "net1.exe",
    #         "powershell.exe",
    #         "pwsh.exe",
    #         "reg.exe",
    #         "regsvr32.exe",
    #         "rundll32.exe",
    #         "wmic.exe",
    #         "wscript.exe"
    #     )
    # 
    #     by Processes.action Processes.dest Processes.original_file_name
    #        Processes.parent_process Processes.parent_process_exec
    #        Processes.parent_process_guid Processes.parent_process_id
    #        Processes.parent_process_name Processes.parent_process_path
    #        Processes.process Processes.process_exec Processes.process_guid
    #        Processes.process_hash Processes.process_id
    #        Processes.process_integrity_level Processes.process_name
    #        Processes.process_path Processes.user Processes.user_id
    #        Processes.vendor_product
    # 
    #     | `drop_dm_object_name(Processes)`
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `windows_sccm_smsexec_spawned_a_suspicious_child_process_filter`
  condition: selection
level: medium
