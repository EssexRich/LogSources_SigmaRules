title: Detection of tools built by NirSoft
id: 7c4db842-7c4d-4c4d-ac4d-7c4db8420000
status: stable
description: Detection rule for MITRE ATT&CK technique T1072
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/detection_of_tools_built_by_nirsoft.yml
author: splunk (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1072
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` count min(_time) values(Processes.process)
    #   as process max(_time) as lastTime from datamodel=Endpoint.Processes where (Processes.process="*
    #   /stext *" OR Processes.process="* /scomma *" ) by Processes.action Processes.dest
    #   Processes.original_file_name Processes.parent_process Processes.parent_process_exec
    #   Processes.parent_process_guid Processes.parent_process_id Processes.parent_process_name
    #   Processes.parent_process_path Processes.process Processes.process_exec Processes.process_guid
    #   Processes.process_hash Processes.process_id Processes.process_integrity_level Processes.process_name
    #   Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    #   | `drop_dm_object_name(Processes)` | `security_content_ctime(firstTime)` |`security_content_ctime(lastTime)`
    #   | `detection_of_tools_built_by_nirsoft_filter`
  condition: selection
level: medium
