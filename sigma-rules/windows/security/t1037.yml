title: Linux MOTD Script Added
id: 2b3ee5a7-2b3e-4b3e-ab3e-2b3ee5a70000
status: stable
description: Detection rule for MITRE ATT&CK technique T1037
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/linux_motd_script_added.yml
author: splunk (via IncidentBuddy)
date: 2026-08-23
tags:
  - attack.t1037
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # -
    #     | tstats `security_content_summariesonly`
    #       count min(_time) as firstTime
    #             max(_time) as lastTime
    # 
    #     from datamodel=Endpoint.Filesystem where
    # 
    #     Filesystem.file_path="/etc/update-motd.d/*"
    #     Filesystem.action IN ("created", "modified")
    # 
    #     by Filesystem.file_path Filesystem.file_name
    #        Filesystem.user Filesystem.dest
    #        Filesystem.action Filesystem.process_guid
    #        Filesystem.process_id
    # 
    #     | `drop_dm_object_name(Filesystem)`
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `linux_motd_script_added_filter`
  condition: selection
level: medium
