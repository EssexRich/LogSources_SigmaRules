title: Linux Docker Privilege Escalation
id: 5c79a346-5c79-4c79-ac79-5c79a3460000
status: stable
description: Detection rule for MITRE ATT&CK technique T1548.003
references:
  - https://github.com/splunk/security_content/blob/develop/detections/deprecated/linux_docker_privilege_escalation.yml
author: splunk (via IncidentBuddy)
date: 2026-03-29
tags:
  - attack.t1548.003
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # -
    #     | tstats `security_content_summariesonly` count min(_time) as firstTime max(_time) as lastTime FROM datamodel=Endpoint.Processes
    #       WHERE Processes.process IN("*docker*-v*/*:*","*docker*--volume*/*:*")
    #         OR
    #         Processes.process IN("*docker*exec*sh*","*docker*exec*bash*")
    #       BY Processes.action Processes.dest Processes.original_file_name
    #          Processes.parent_process Processes.parent_process_exec Processes.parent_process_guid
    #          Processes.parent_process_id Processes.parent_process_name Processes.parent_process_path
    #          Processes.process Processes.process_exec Processes.process_guid
    #          Processes.process_hash Processes.process_id Processes.process_integrity_level
    #          Processes.process_name Processes.process_path Processes.user
    #          Processes.user_id Processes.vendor_product
    #     | `drop_dm_object_name(Processes)`
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `linux_docker_privilege_escalation_filter`
  condition: selection
level: medium
