title: Advanced IP or Port Scanner Execution
id: 0c9b1187-0c9b-4c9b-ac9b-0c9b11870000
status: stable
description: Detection rule for MITRE ATT&CK technique T1046
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/advanced_ip_or_port_scanner_execution.yml
author: splunk (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1046
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly`
    #     count min(_time) as firstTime
    #           max(_time) as lastTime
    # 
    #     from datamodel=Endpoint.Processes where
    #       Processes.process_name IN ("advanced_ip_scanner.exe", "advanced_ip_scanner_console.exe", "advanced_port_scanner.exe", "advanced_port_scanner_console.exe")
    #       OR
    #       Processes.original_file_name IN ("advanced_ip_scanner.exe", "advanced_ip_scanner_console.exe", "advanced_port_scanner.exe", "advanced_port_scanner_console.exe")
    #       OR (
    #       Processes.process = "* /portable *"
    #       Processes.process = "* /lng *"
    #       )
    # 
    #     by Processes.action Processes.dest Processes.original_file_name Processes.parent_process
    #        Processes.parent_process_exec Processes.parent_process_guid Processes.parent_process_id
    #        Processes.parent_process_name Processes.parent_process_path Processes.process Processes.process_exec
    #        Processes.process_guid Processes.process_hash Processes.process_id Processes.process_integrity_level
    #        Processes.process_name Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    # 
    #   | `drop_dm_object_name(Processes)`
    #   | `security_content_ctime(firstTime)`
    #   | `security_content_ctime(lastTime)`
    #   | `advanced_ip_or_port_scanner_execution_filter`
  condition: selection
level: medium
