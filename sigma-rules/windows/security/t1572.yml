title: Socat Remote TCP Connection with Local Echo Disabled
id: 3e9362d3-3e93-4e93-ae93-3e9362d30000
status: stable
description: Detection rule for MITRE ATT&CK technique T1572
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/socat_remote_tcp_connection_with_local_echo_disabled.yml
author: splunk (via IncidentBuddy)
date: 2026-09-13
tags:
  - attack.t1572
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # -
    #     | tstats `security_content_summariesonly`
    #       count min(_time) as firstTime
    #             max(_time) as lastTime
    # 
    #     from datamodel=Endpoint.Processes where
    # 
    #     Processes.process_name="socat*"
    #     Processes.process="*echo=0*"
    #     Processes.process IN (
    #         "*openssl:*",
    #         "*tcp:*",
    #         "*tcp4:*",
    #         "*tcp6:*"
    #     )
    # 
    #     by Processes.dest Processes.original_file_name Processes.parent_process_id
    #        Processes.process Processes.process_exec Processes.process_guid
    #        Processes.process_hash Processes.process_id
    #        Processes.process_current_directory Processes.process_name
    #        Processes.process_path Processes.user
    #        Processes.user_id Processes.vendor_product
    # 
    #     | `drop_dm_object_name(Processes)`
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `socat_remote_tcp_connection_with_local_echo_disabled_filter`
  condition: selection
level: medium
