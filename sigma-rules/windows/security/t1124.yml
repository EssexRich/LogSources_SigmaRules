title: Windows System Time Discovery W32tm Delay
id: 650f0870-650f-450f-a50f-650f08700000
status: stable
description: Detection rule for MITRE ATT&CK technique T1124
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/windows_system_time_discovery_w32tm_delay.yml
author: splunk (via IncidentBuddy)
date: 2026-02-01
tags:
  - attack.t1124
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` values(Processes.process) as process
    #   min(_time) as firstTime max(_time) as lastTime from datamodel=Endpoint.Processes
    #   where Processes.process_name = w32tm.exe Processes.process= "* /stripchart *" Processes.process=
    #   "* /computer:localhost *" Processes.process= "* /period:*" Processes.process= "*
    #   /dataonly *" Processes.process= "* /samples:*" by Processes.action Processes.dest
    #   Processes.original_file_name Processes.parent_process Processes.parent_process_exec
    #   Processes.parent_process_guid Processes.parent_process_id Processes.parent_process_name
    #   Processes.parent_process_path Processes.process Processes.process_exec Processes.process_guid
    #   Processes.process_hash Processes.process_id Processes.process_integrity_level Processes.process_name
    #   Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    #   | `drop_dm_object_name(Processes)` | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`
    #   | `windows_system_time_discovery_w32tm_delay_filter`
  condition: selection
level: medium
