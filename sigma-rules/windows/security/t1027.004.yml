title: CSC Net On The Fly Compilation
id: 7d9af7f7-7d9a-4d9a-ad9a-7d9af7f70000
status: stable
description: Detection rule for MITRE ATT&CK technique T1027.004
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/csc_net_on_the_fly_compilation.yml
author: splunk (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1027.004
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` count min(_time) as firstTime max(_time)
    #   as lastTime from datamodel=Endpoint.Processes where
    #   (Processes.process_name=csc.exe OR Processes.original_file_name=csc.exe)
    #   Processes.process = "*/noconfig*"
    #   Processes.process = "*/fullpaths*"
    #   Processes.process = "*@*"
    #   by Processes.action Processes.dest Processes.original_file_name Processes.parent_process
    #      Processes.parent_process_exec Processes.parent_process_guid Processes.parent_process_id
    #      Processes.parent_process_name Processes.parent_process_path Processes.process Processes.process_exec
    #      Processes.process_guid Processes.process_hash Processes.process_id Processes.process_integrity_level
    #      Processes.process_name Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    #   | `drop_dm_object_name(Processes)` | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`
    #   | `csc_net_on_the_fly_compilation_filter`
  condition: selection
level: medium
