title: Detect Certify Command Line Arguments
id: 7bc23cf7-7bc2-4bc2-abc2-7bc23cf70000
status: stable
description: Detection rule for MITRE ATT&CK technique T1649
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/detect_certify_command_line_arguments.yml
author: splunk (via IncidentBuddy)
date: 2026-01-28
tags:
  - attack.t1649
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` count min(_time) as firstTime max(_time)
    #   as lastTime from datamodel=Endpoint.Processes where Processes.process IN ("* find
    #   *","* auth *","* request *","* req *","* download *",) AND Processes.process IN
    #   ("* /vulnerable*","* /enrolleeSuppliesSubject *","* /json /outfile*","* /ca*", "*
    #   -username *","* -u *") by Processes.action Processes.dest Processes.original_file_name
    #   Processes.parent_process Processes.parent_process_exec Processes.parent_process_guid
    #   Processes.parent_process_id Processes.parent_process_name Processes.parent_process_path
    #   Processes.process Processes.process_exec Processes.process_guid Processes.process_hash
    #   Processes.process_id Processes.process_integrity_level Processes.process_name Processes.process_path
    #   Processes.user Processes.user_id Processes.vendor_product | `drop_dm_object_name(Processes)`
    #   | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`| `detect_certify_command_line_arguments_filter`
  condition: selection
level: medium
