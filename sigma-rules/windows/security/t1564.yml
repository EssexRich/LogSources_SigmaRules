title: Windows New Deny Permission Set On Service SD Via Sc.EXE
id: 072c6746-072c-472c-a72c-072c67460000
status: stable
description: Detection rule for MITRE ATT&CK technique T1564
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/windows_new_deny_permission_set_on_service_sd_via_sc_exe.yml
author: splunk (via IncidentBuddy)
date: 2026-02-08
tags:
  - attack.t1564
logsource:
  product: windows
  service: security
detection:
  selection:
    # Original splunk detection logic
    # | tstats `security_content_summariesonly` values(Processes.process) as process
    #   min(_time) as firstTime max(_time) as lastTime from datamodel=Endpoint.Processes
    #   where (Processes.process_name=sc.exe OR Processes.original_file_name=sc.exe) Processes.process="*sdset
    #   *" Processes.process="*(D;*" Processes.process IN ("*;IU*", "*;S-1-5-4*", "*;SU*",
    #   "*;S-1-5-6*", "*;BA*", "*;S-1-5-32-544*", "*;SY*", "*;S-1-5-18*", "*;WD*", "*;S-1-1-0*",
    #   "*;AU*", "*;S-1-5-11*", "*;LS*", "*;S-1-5-19*") by Processes.action Processes.dest
    #   Processes.original_file_name Processes.parent_process Processes.parent_process_exec
    #   Processes.parent_process_guid Processes.parent_process_id Processes.parent_process_name
    #   Processes.parent_process_path Processes.process Processes.process_exec Processes.process_guid
    #   Processes.process_hash Processes.process_id Processes.process_integrity_level Processes.process_name
    #   Processes.process_path Processes.user Processes.user_id Processes.vendor_product
    #   | `drop_dm_object_name(Processes)` | `security_content_ctime(firstTime)` | `security_content_ctime(lastTime)`
    #   | `windows_new_deny_permission_set_on_service_sd_via_sc_exe_filter`
  condition: selection
level: medium
