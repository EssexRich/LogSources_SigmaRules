title: Windows Defender Threat Detected on Kernel Object Path
id: 1972e864-1972-4972-a972-1972e8640000
status: stable
description: Detection rule for MITRE ATT&CK technique T1211
references:
  - https://github.com/splunk/security_content/blob/develop/detections/endpoint/windows_defender_threat_detected_on_kernel_object_path.yml
author: splunk (via IncidentBuddy)
date: 2026-08-23
tags:
  - attack.t1211
logsource:
  product: defender
  service: xdr
detection:
  selection:
    # Original splunk detection logic
    # -
    #     `ms_defender`
    #     EventCode IN (1116, 1117)
    #     Path="*\\globalroot\\*"
    #     | fillnull
    #     | stats count min(_time) as firstTime
    #                   max(_time) as lastTime
    #                   values(EventCode) as EventCode
    #                   values(Action_Name) as Action_Name
    #                   values(Additional_Actions_String) as Additional_Actions_String
    #                   values(FWLink) as FWLink
    #       by dvc Detection_User Path Threat_Name Severity_Name Category_Name
    #     | rename dvc as dest, Detection_User as user
    #     | `security_content_ctime(firstTime)`
    #     | `security_content_ctime(lastTime)`
    #     | `windows_defender_threat_detected_on_kernel_object_path_filter`
  condition: selection
level: medium
