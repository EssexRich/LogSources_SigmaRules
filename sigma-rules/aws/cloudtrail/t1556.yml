title: AWS IAM Account Password Policy Deleted
id: 2704a5df-2704-4704-a704-2704a5df0000
status: stable
description: Detection rule for MITRE ATT&CK technique T1556
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/integrations/aws/defense_evasion_iam_account_password_policy_deleted.toml
author: elastic (via IncidentBuddy)
date: 2026-07-12
tags:
  - attack.t1556
logsource:
  product: aws
  service: cloudtrail
detection:
  selection:
    # Original elastic detection logic
    # data_stream.dataset: "aws.cloudtrail"
    #     and event.provider: "iam.amazonaws.com"
    #     and event.action: "DeleteAccountPasswordPolicy"
    #     and event.outcome: "success"
    #     and not aws.cloudtrail.user_identity.type: "AWSService"
    #     and not user_agent.original: (*terraform* or *pulumi* or *ansible*)
    #     and not aws.cloudtrail.user_identity.arn: (*terraform* or *pulumi* or *ansible*)
    #     and not source.address: ("cloudformation.amazonaws.com" or "servicecatalog.amazonaws.com")
  condition: selection
level: medium
